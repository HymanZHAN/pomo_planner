/**
 * This module is the place for all shared domain dependencies, including
 *   - common base classes (e.g. AggregateRoot, Entity, ValueObject)
 *   - common interfaces
 * and so on.
 *
 */

export { AggregateRoot } from "./aggregate-root";
export { Entity } from "./entity";
export { ValueObject } from "./value-object";

export { AbstractRepository } from "./repository";
